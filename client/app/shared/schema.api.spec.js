import {SchemaAPI} from './schema.api';

describe('Schema API', () => {
  let $q;
  let $log;
  beforeEach(inject(_$q_,_$log_)=> {
    $q = _$q_
    $log = _$log_;

  });
  describe('Field type API', () => {
    it('should list all types', () => {

    });
    it('should add type', () => {

    });
    it('should remove type', () => {

    });
    it('should replace type', () => {

    });
  });
});
